# FDTD 2D

There are a couple simulation files. Compile and run whichever you want. 

Afterwards, to visualuze the results, run `python save.py`. Two video files should appear, alongside a folder containing the frames of the video.

To delete everything, just run `./clear.sh`


